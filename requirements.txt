# DSPyUI Dependencies
# Python 3.10+ required (DSPy 3.0 requirement)

# Core UI Framework
gradio>=5.0.0

# Data Processing
pandas>=2.0.0

# LLM API Clients
openai>=1.0.0
anthropic>=0.40.0
groq>=0.12.0
google-generativeai>=0.8.0

# Machine Learning
scikit-learn>=1.3.0

# DSPy Framework
# Using pip-installed version instead of vendored due to import issues in beta
dspy-ai>=3.0.0

# Note: A vendored copy exists in ./dspy/ directory (version 3.0.4b1) but has circular import issues
# The pip-installed version is more stable

# Development Tools
mypy>=1.18.0  # Type checking
pydantic>=2.0.0  # Data validation (already included via dspy-ai but pinning explicitly)
